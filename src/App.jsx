import { useState } from 'react'
import './App.css'

// Project Structure
// Let's start by setting up a Vite React project and planning the components needed:

// Create a new Vite+React project
// Set up Cloudflare Workers for handling the speed test
// Build a simple UI with a "Start Test" button and results display

// How Speed Tests Work
// Speed tests typically work by:

// Downloading files of known sizes
// Measuring how long it takes to download them
// Calculating the speed (bytes/second)

function App() {
  return (
    <div>
      <header>hi</header>
    </div>
  )
}

export default App
